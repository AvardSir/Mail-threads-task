// src/exporter.ts
// ---- 1. Imports ----
// (Milestone 7 will add: import { getAllMessages } from './db';)

// ---- 2. Public API ----
/**
 * Stub for Milestone 5. Real implementation lands in Milestone 7:
 * reads getAllMessages() and writes ./out/result.jsonl.
 */
export const exportAll = async (): Promise<void> => {
  return;
};